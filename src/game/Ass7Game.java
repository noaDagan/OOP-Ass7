package game;

import animation.AnimationRunner;
import biuoop.GUI;
import invaders.AlienClass;
import levels.LevelSetReader;
import shapes.Rectangle;

/**
 * The Ass7Game class.
 */
public class Ass7Game {
    /**
     * The main menu.
     *
     * @param args a string
     * @throws Exception w wrong value
     */
    public static void main(String[] args) throws Exception {
//        AlienClass block = (AlienClass) new Block(null, null);
//        System.out.println(block.getCollisionRectangle().getUpperLeft().getX());

//        Block b = (Block) new AlienClass(null, null, null);
//        System.out.println(b.getCollisionRectangle().getUpperLeft().getX());

        Object o = new AlienClass(null, null, null));
        System.out.println();
//        double speedAlien = 2;
//        String levelSetsPath = "level_sets.txt";
//        GUI gui = new GUI("new game", 800, 600);
//        AnimationRunner runner = new AnimationRunner(gui);
//        GameFlow gameFlow = new GameFlow(runner, gui.getKeyboardSensor(), 3, gui, speedAlien);
//        LevelSetReader levelSet = new LevelSetReader();
//        gameFlow.runTheMenu(levelSet);
//        gui.close();

    }
}
